[package]
name = "mercs2_physics"
version = "1.0.1"
edition = "2021"
description = "Mercenaries 2 reimpl — Havok character controller, rigid bodies, collision queries, MOPP/heightfield."
readme = "README.md"
license.workspace = true
repository.workspace = true

[dependencies]
mercs2_core = { workspace = true }
mercs2_formats = { workspace = true }
