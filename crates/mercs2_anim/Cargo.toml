[package]
name = "mercs2_anim"
version = "1.1.1"
edition = "2021"
description = "Mercenaries 2 reimpl — animation runtime (controllers, IK, ragdoll, FaceFX)."
readme = "README.md"
license.workspace = true
repository.workspace = true

[dependencies]
mercs2_core = { workspace = true }
mercs2_formats = { workspace = true }
