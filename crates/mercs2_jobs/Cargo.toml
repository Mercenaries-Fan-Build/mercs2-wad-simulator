[package]
name = "mercs2_jobs"
version = "1.0.2"
edition = "2021"
description = "Mercenaries 2 reimpl — Pimp job system: worker pool + CS-guarded job ring, the engine's parallel-work spine."
readme = "README.md"
license.workspace = true
repository.workspace = true

[dependencies]
mercs2_core = { workspace = true }
mercs2_formats = { workspace = true }
