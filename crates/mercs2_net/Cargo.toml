[package]
name = "mercs2_net"
version = "1.0.1"
edition = "2021"
description = "Mercenaries 2 reimpl — networking (Keystone-B replication, session/transport, FESL)."
readme = "README.md"
license.workspace = true
repository.workspace = true

[dependencies]
mercs2_core = { workspace = true }
mercs2_formats = { workspace = true }
