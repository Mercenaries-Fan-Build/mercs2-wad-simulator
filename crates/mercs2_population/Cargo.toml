[package]
name = "mercs2_population"
version = "1.0.2"
edition = "2021"
description = "Mercenaries 2 reimpl — Population / spawners: SkirmishSpawnList, living-world crowd/traffic density, dynamic-road flow. Feeds AI actors through the spawn resolver."
readme = "README.md"
license.workspace = true
repository.workspace = true

[dependencies]
mercs2_core = { workspace = true }
mercs2_formats = { workspace = true }
